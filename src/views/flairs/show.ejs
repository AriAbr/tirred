<!DOCTYPE html>
<html>
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
      <h1><button type="button" class="btn btn-lg btn-primary" disabled
      style="
        margin-top: 15px;
        background-color: <%= flair.color %>;
        border-width: 0px;
        border-radius: 7px 50px 50px 7px;
      "><h1> ⚞ <%= flair.name %></h1></button></h1>

      <a href="/topics/<%= flair.topicId %>/flairs/<%= flair.id %>/edit" class="btn btn-warning">Edit</a>
      <form style="display:initial" action="/topics/<%= flair.topicId %>/flairs/<%= flair.id %>/destroy" method="post">
        <button type="submit" class="btn btn-danger">Delete</button>
      </form>
    </main>

    <% include ../static/partials/baseScripts.ejs %>

  </body>
</html>
